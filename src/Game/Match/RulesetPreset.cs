namespace MphRead
{
    public enum RulesetPreset : byte { Classic, Competitive, Duel, Custom }
    public enum RankingEligibility : byte { Unranked, VerifiedServerOnly }
    public enum RadarPolicy : byte { Classic, Disabled, Enabled }
    public enum TeamBalancePolicy : byte { BeforeStart, Locked }
}
