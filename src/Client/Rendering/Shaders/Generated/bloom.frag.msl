#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_BloomConstants
{
    float4 bloomOptions;
    float4 texelSize;
};

struct main_ps_out
{
    float4 out_var_SV_Target0 [[color(0)]];
};

struct main_ps_in
{
    float2 in_var_TEXCOORD0 [[user(locn0)]];
};

fragment main_ps_out main_ps(main_ps_in in [[stage_in]], constant type_BloomConstants& BloomConstants [[buffer(0)]], texture2d<float> sourceTexture [[texture(0)]], sampler sourceSampler [[sampler(0)]])
{
    main_ps_out out = {};
    float4 _107;
    do
    {
        if (dot(BloomConstants.bloomOptions.xy, BloomConstants.bloomOptions.xy) < 0.5)
        {
            _107 = float4(sourceTexture.sample(sourceSampler, in.in_var_TEXCOORD0).xyz * BloomConstants.bloomOptions.z, 0.0);
            break;
        }
        float2 _63 = BloomConstants.bloomOptions.xy * BloomConstants.texelSize.xy;
        float2 _88 = _63 * 2.0;
        _107 = float4(((((sourceTexture.sample(sourceSampler, in.in_var_TEXCOORD0).xyz * 0.375) + (sourceTexture.sample(sourceSampler, (in.in_var_TEXCOORD0 + _63)).xyz * 0.25)) + (sourceTexture.sample(sourceSampler, (in.in_var_TEXCOORD0 - _63)).xyz * 0.25)) + (sourceTexture.sample(sourceSampler, (in.in_var_TEXCOORD0 + _88)).xyz * 0.0625)) + (sourceTexture.sample(sourceSampler, (in.in_var_TEXCOORD0 - _88)).xyz * 0.0625), 0.0);
        break;
    } while(false);
    out.out_var_SV_Target0 = _107;
    return out;
}

