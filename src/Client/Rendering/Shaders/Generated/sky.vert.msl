#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main_vs_out
{
    float2 out_var_TEXCOORD0 [[user(locn0)]];
    float4 gl_Position [[position]];
};

vertex main_vs_out main_vs(uint gl_VertexIndex [[vertex_id]])
{
    main_vs_out out = {};
    float2 _35;
    if (gl_VertexIndex == 0u)
    {
        _35 = float2(-1.0);
    }
    else
    {
        _35 = select(float2(-1.0, 3.0), float2(3.0, -1.0), bool2(gl_VertexIndex == 1u));
    }
    out.gl_Position = float4(_35, 0.0, 1.0);
    out.out_var_TEXCOORD0 = float2((_35.x * 0.5) + 0.5, 0.5 - (_35.y * 0.5));
    return out;
}

