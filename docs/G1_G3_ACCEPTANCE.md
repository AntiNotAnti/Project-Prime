# G1–G3 acceptance audit

Audit of the supplied `PRIME_HUNTERS_G1_G5_IMPLEMENTATION_PLAN.md` against the shared working tree on 2026-09-07. This is a source/evidence audit, not a new full-suite or device run. G1–G3 must not be marked fully accepted yet. The table distinguishes implemented paths, bounded exclusions, and validation that needs a real renderer/device. Current audio routes are included below; audible and assembled-run acceptance remain separate gates.

## Actionable code and contract gaps

| Priority | Requirement | Concrete current evidence | Closure |
|---|---|---|---|
| Closed in follow-up | G1.2 doors and simple dynamic animation | `NodePoseSampler` and `ModelPoseHistory` capture authored door and platform node poses at completed simulation ticks; `ResolveNodeSubmission` copies interpolated submission matrices without advancing shared model/collision state. `NodePoseInterpolationTests` covers authored-door LUT/skin-stack restoration and reset behavior. | Bounded implementation is present. Focused render evidence is 18/18, including platform nodes and independent sprite effects. Player skeletal animation, owner-relative/mesh/persistent effects, and retail visual acceptance remain outside this scope. |
| Closed named storage gap | G1.1 authoritative node/projectile timers | Node capture/score and beam life/age/decay now store integer ticks; immutable compatibility projections preserve exact old float interpolation/wire values. | Fourth-pass `G1_TIMING.md`: 61 timing tests pass, including14 new boundary/real-content/trajectory cases; catch-up and homing harnesses pass. Complete rendered golden-match trace remains a separate gate. |
| Implemented in follow-up | G2.5 recap always available after match/replay | `RecapArchive` retains up to 16 life records with up to eight rows each, updates matching previous-life records when delayed facts arrive, and clears on match/session/view-owner changes. Post-match and modern replay navigation expose the archive without generating gameplay input. | Focused recap/result/navigation tests pass; Android has contextual RECAP/CLOSE and NEXT/PREV touch controls with input suppressed while browsing. Rendered layout and touch ergonomics remain device checks. |
| Closed in follow-up | G3.3 reconnect grace | `ServerNetwork` now keeps a bounded 1,800-tick reservation for an actual participant after disconnect, retaining slot/team/stat identity while releasing the gameplay body and carried objectives. Re-entry requires the previous connection ID, same endpoint, and unchanged hunter/name; `LateJoinTests` covers return, expiry, endpoint rejection, rotation clearing, non-participant rejection, and eliminated-survival spectator return. | Same-endpoint reconnect and match fencing are implemented. Focused G4 ticket admission also proves fresh-ticket same-PlayerId recovery across a changed client endpoint; deployed/backend identity and transport proof remain separate. |
| Implemented in follow-up | G3.5 newest valid weapon intent | `PlayerPresentationWeaponSelection.ApplyWeaponSelection` routes nine direct weapons, cycle, pointer/radial, QuickSwap, and `Bindings.AffinitySlot` through the same newest-valid intent queue. The server still receives the actually equipped weapon. | `NetworkActionIntegrationTests.RepeatedUdpDesiredWeaponWaitsThroughGunTransitionAndTracksPreviousWeapon` passes over an impaired UDP link, including actual equip and `PreviousWeapon` bookkeeping after `UpDown`. Device ergonomics remain a validation gate. |
| Implemented in follow-up | G3.5 selection wedge preview | `DrawWeaponRadial` draws nine bounded annular sectors behind the radial labels and highlights the selected preview sector. Pure sector geometry and shared-boundary behavior are covered by the focused touch/radial cases. | The wedge path is implemented without per-frame geometry-array allocation. Controller/touch readability and ergonomics remain a separate device gate. |
| Closed in bounded scope | G1.2 phase-2 cosmetic effects | `ParticlePoseHistory` interpolates independent world-space effect sprites captured after existing effect ticks, including catch-up steps, without advancing effects at render frequency. Pool checkout and scene reset clear identity history. | Owner-relative effects, mesh effects, player skeletal animation, complex attachments, and persistent effects remain explicit exclusions pending a typed presentation contract. Focused render tests cover source-state preservation, repeated draws, pool reuse, and exclusions. |
| Implemented in follow-up | G2.8/2.9 overtime/match-point/major-respawn cues | `WorldFeedback` emits typed overtime/match-point announcements and opt-in major-pickup respawn notifications; `FeedbackAudio` maps distinct cues, applies cooldown/mute/volume policy, and preserves positional respawn audio. `FeedbackAudioTests` and `WorldFeedbackAnnouncementTests` cover cue identity, spacing, mute, health hysteresis, ordinary-pickup silence, opt-in major respawns, overtime, and match point. | Source and focused evidence are present. Native listening, mix balance, spatial audibility, and assembled live-event timing remain separate gates. |

G3 browser shows actual custom rules, named ruleset presets and unverified/public status. `RulesetResolver` and the Protocol 8 status extension now carry Classic, Competitive, Duel and Custom preset identity; `ServerBrowser` still labels verification as unchecked rather than treating a server claim as proof. Focused G4 admission coverage also proves same-account reconnect across a changed client endpoint with a fresh signed ticket and PlayerId binding. Deployed backend ranking, credential revocation and official verification remain G4/G5 gates. Optional vertical damage hints, custom radar reveal behavior, casual score-based team tie-breaking, parties and MMR are not mandatory defaults for this pass.

## Per-section acceptance checklist

Legend: **Implemented** means concrete code exists, not that all acceptance tests are complete. “Bounded” identifies an explicit scope exclusion or an unclosed validation gate. **Evidence-only rejection** is an intentional experiment outcome.

| Section | Current disposition | Evidence / remaining acceptance |
|---|---|---|
| G1.0 baseline | Recorded with failures | `G1_BASELINE.md`, `G1_NETWORK_BASELINE.md`: frozen b31bc57, 434 main/18 imaging/47 Python, WAN matrices and platform limits. Missing native frames and some emitted diagnostic baselines remain unavailable, not zero. |
| G1.1 timing | Named storage gaps implemented; trace gate | SimTicks plus four migration groups;61 focused timing cases, real node capture and projectile trajectories, catch-up/homing parity. Integer node/beam clocks replace the former storage deferral. This is not complete rendered golden-match trace parity. |
| G1.2 interpolation | Implemented bounded; visual gates | Scene-owned two-sample history, copied submissions, camera/viewmodel, platform and door node poses, bomb/beam/item roots, independent world-space sprites, remote exclusion, and explicit discontinuities. `NodePoseInterpolationTests` and `ParticleInterpolationTests` cover the bounded node/effect paths; focused render evidence is 18/18. Player skeletal animation, owner-relative/mesh/persistent effects, and real 60-vs-240 world hash/collision/shot traces remain open. |
| G1.3 mouse late latch | Implemented; device gate | One desktop accumulator, nonconsuming draw peek, single simulation consume, reset/stale bounds. 120/144/240 synthetic input conservation tests. Native captured-mouse event semantics and perceived latency not proven. Controller late latch correctly deferred pending mouse proof. Android keeps its existing single destructive touch consume. |
| G1.4 afflictions | Implemented | Protocol8AfflictionTests and player snapshot/presentation reconciliation; old layouts remain frozen. Real burn/disrupt/freeze visuals after loss/recovery still need client acceptance. |
| G1.5 input edges | Transport/model evidence | InputEdgeStressTests runs all seven named edges through InputBundle decode and ServerInputStream under clean/loss/burst/jitter/reorder/stall schedules; bounded loss is accounted. It does not drive all seven real Player actions over a UDP client/server match. Add that integration proof before claiming action-level exactly-once. |
| G1.6 ordering | Implemented characterization | `SIMULATION_ORDERING.md`, real-content SimulationOrderingCheck covers specified simultaneous cases with narrow in-step action fixtures. It does not substitute for full projectile flight; separate combat harnesses cover that path. |
| G1.7 spawning | Implemented policies | SpawnDirector/Classic/Enhanced/Duel, history, deterministic choice and explicit offensive-action protection option; SpawnDirectorTests and ServerSpawnOptionsTests. Preserve Classic. Review source-backed diagnostics and eight-player/fallback fixtures in final assembled run. |
| G1.8 lifecycle | Implemented fences | MatchLifecycleTests, socket LifecycleTests, match-phases and ordering harness cover countdown/quorum, stale epochs, terminal freeze and rotation. Re-run assembled protocol/content build; historical repaired fixtures are separate from frozen baseline. |
| G2.1–2.3 feedback/markers/feed | Implemented | Identity-safe bounded families, dedicated Kill with SourceKind; positive/silent/zero policy, roster connection names, assists/affinity. CombatFeedbackTests. UI/audio alignment and live delayed-projectile readability remain graphical gates. |
| G2.4 assists | Implemented | Bounded server contribution ledger, actual health damage, dedicated Kill and immutable Assists; AssistAttributionTests/AssistResultTests/KillDeliveryTests. Classic score unchanged. |
| G2.5 recap | Implemented follow-up | Bounded current-life plus retained-life archive, post-match pages and modern replay navigation. 37 combined recap/feedback/result tests pass; device navigation remains a gate. |
| G2.6 damage direction | Implemented | Eight sectors and all boundary angles tested; optional vertical hint deferred. |
| G2.7 radar | Implemented bounded presentation | Prepared legal contacts, classic/enhanced, heading/north, elevation/clamp, capacity64. Seven radar tests documented. Small-screen/Android graphical acceptance remains mandatory. |
| G2.8–2.9 objective/audio | Implemented; listening gate | Typed world-event bus and `FeedbackAudio` independent gain/cooldowns are wired for objective, Prime, overtime, match point, and opt-in major respawns. `FeedbackAudioTests` and `WorldFeedbackAnnouncementTests` cover cue identity, spacing/mute, health hysteresis, ordinary-pickup silence, major-respawn opt-in, overtime, and match point. Actual SFX distinction, position, masking, overload, and audible mix quality still need listening/full integration checks. |
| G2.10 results | Implemented model/UI | Immutable result pages use actual DamageDealt, headshot kills, streak, best beam, mode counters. Two focused tests distinguish frozen actual damage from old beam-efficiency numerator. Headshots currently means headshot kills, labeled explicitly; clarify if all headshot hits are required. Eight-player legibility and enough page dwell before rotation need rendered acceptance. |
| G3.1 overtime | Implemented opt-in | Playing+MatchPeriod, mode-aware tie/contest policy, explicit disabled Classic default, replicated state. OvertimePolicyTests cover 12 modes; content nettest OvertimeCheck exists. `G3_OVERTIME.md` has stale in-progress validation prose: refresh against latest assembled run. |
| G3.2 teams | Implemented | Count-based admission, loading reservations, deterministic prestart rebalance/minimal moves. Exhaustive 6561 configurations documented. No mid-match auto shuffle. |
| G3.3 late joining | Implemented bounded reconnect; G4 boundary | Next-match waiting slots are inactive, excluded from quorum/scoring and locked in client spectator view; ready-boundary and disabled policy are tested. Actual participants retain a 1,800-tick same-endpoint reconnect reservation with slot/team/stat continuity, expiry and rotation fencing; `LateJoinTests` covers return and rejection cases. Focused `TicketAdmissionTests` separately proves changed-endpoint authenticated recovery with a fresh signed ticket and the same PlayerId. Named Competitive/Duel presets are implemented and resolved by `RulesetResolver`; official backend ranking/verification remains outside this G3 acceptance. |
| G3.4 world events | Implemented presentation/recovery split | Type12 WorldEvent64, stable item IDs, match/phase fences, bounded admission/dedup. Full world remains sole gameplay recovery; events do not grant pickups twice. WorldEventTests/WorldFeedbackTests and integration delivery. Item visual disappearance still follows snapshot state; immediate HUD/audio is what current events accelerate. |
| G3.5 weapons | Implemented bounded; device gate | Affinity selection now enters the same newest-valid intent queue as direct/cycle/QuickSwap/radial input, and the nine-sector annular wedge preview is implemented. Pure queue/radial tests pass; the impaired-UDP desired-weapon regression covers actual equip, transition retry and `PreviousWeapon` bookkeeping. No cooldown tuning is claimed; device ergonomics remain. |
| G3.6 network HUD | Implemented | Existing NetMetrics only; diagnostic thresholds, missing sample `--`, degraded-only default, optional advanced. Six threshold tests. Server starvation display is optional and not replicated into this HUD. |
| G3.7 adaptive | Evidence-only rejection | See experiment below; fixed6 retained, no speculative runtime policy. |
| G3.8 browser | Implemented with truthful unavailable fields | Favorites32/recents16/filter/sort/quick join/fresh probe, optional rules discovery tail, and current preset/rules fields. `ServerBrowserTests` covers eligibility/bounds/base+legacy+extended/malformed tails and v1/v2/v3 preset/status compatibility; verification remains explicitly unchecked. Launcher interaction and favorites persistence smoke remain distinct from pure tests. |

## Adaptive experiment disposition

`G3_INTERPOLATION_EXPERIMENT.md` records 35 deterministic fixed6 versus bounded3–6 comparisons. Only25 met all predeclared gates; downlink-heavy extrapolation rose from0.17% to49.54%. Two outputs were byte-identical. This supports rejecting that RTT-symmetry candidate under G3's explicit “proven or rejected” acceptance. It does not certify every adaptive design as bad. CPU/bandwidth were proxy estimates, and fairness used an idealized clock/profile model; no production profile/lag compensation change should be made from that experiment.

## Known failures and hardware-only gates

- Frozen long mixed runs exposed reliable queue overflow and scheduling drops. Overflow was absent on an unchanged repeat; concurrent builds confound timing attribution. Diagnostics were improved, but do not overwrite baseline observations or claim a soak pass without rerunning under a controlled workload.
- Frozen lifecycle/catch-up/homing harness failures were stale fixture assumptions. Fixture-only repairs in34d3549 retained assertions and passed; they are not unexplained gameplay regressions.
- Native macOS render probe failed before gameplay with `NSGL: The compatibility profile is not available on macOS`. No real high-refresh frame trace, rendered demo or mouse-to-photon proof exists. Windows/Linux capable GL or a supported native renderer is needed for those gates.
- Android managed/APK builds establish compilation and packaging only. Actual touch/controller, eight-player HUD/feed/radar/results layout, audible cue balance and high-refresh visual checks require devices.
- Tmds.DBus.Protocol0.21.2 NU1903 remains a known dependency warning. Track separately from gameplay acceptance.

## Before advancing persistence claims

The bounded code gaps identified in this audit are closed for the documented door/platform node path, independent world-space sprites, audio routes, reconnect reservation, affinity intent, selection wedge, and impaired-UDP desired-weapon transition. The final assembled main suite now passes 954/954, Imaging 18/18, Python 58/58, and the solution, dedicated publish, Android managed build and real-content harnesses pass; see [integration evidence](G1_G5_VALIDATION.md). Native renderer/device checks still remain for visual layout, touch/controller ergonomics, high-refresh timing, and audible mix/spatial quality. Keep G4 identity work separable: same-endpoint reconnect and focused fresh-ticket PlayerId recovery across endpoint changes are implemented, while deployed revocation, official ranking and production transport proof remain G4 gates. G1–G3 completion must remain conditional, and `G1_G5_PROGRESS.md` should be updated from final run evidence rather than this source audit alone.

This document reconciliation does not modify production, LICENSE, maps or generated content. The final suite is recorded separately from this source audit; no live-device result is claimed.
