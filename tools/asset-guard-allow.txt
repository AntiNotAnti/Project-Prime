# Paths the asset guard may ignore, one glob per line.
#
# Only for files that are ours: a logo, launcher artwork, an original sound,
# or a diagram. Never a map preview, extracted model/sound, or anything else
# that came out of the cartridge -- those are produced on each player's own
# machine and do not belong in this repository or in a release.

# Project Prime's renderer-owned visual marks. The standard mark is embedded
# for the game window and is also the source used by macOS packaging; the
# server variant adds the dedicated-server badge.
src/MphRead/Assets/project-prime-mark.png
src/MphRead/Assets/project-prime-mark-server.png
src/MphRead/Assets/project-prime-banner.png
src/MphRead/Assets/project_prime_icon.png

# Supplied Project Prime startup artwork. Keep the exact path; locally
# extracted game previews are still excluded.
src/MphRead/Assets/Backgrounds/launcher-bg.png

# Original Project Prime combat-feedback stingers. These are programmatically
# synthesized for this project and contain no cartridge audio or third-party samples.
src/MphRead/Assets/Sounds/Combat/*.wav

# Generated production-codec benchmark telemetry for 2,016 seeded scenarios.
# Contains numerical test results only, no extracted game data; exceeds 2 MiB.
tools/nettest/baselines/network-v18.json
